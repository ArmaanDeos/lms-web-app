import { Outlet } from "react-router-dom";

const CommonStudentLayout = () => {
  return (
    <div>
      CommonStudentLayout
      <Outlet />
    </div>
  );
};

export default CommonStudentLayout;
